function gcd(firsNum, secondNumber){

    let gcd = 0;

    for (let i = 1; i <= firsNum && i <= secondNumber; i++) {
        if (firsNum % i === 0 && secondNumber % i ===0) {
            gcd = i;
        }
    }

    return gcd;
}
